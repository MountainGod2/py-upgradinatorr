version: '3.8'

services:
  upgradinatorr:
    image: mountaingod2/upgradinatorr:develop
    container_name: upgradinatorr
    environment:
      - PUID=1000
      - PGID=1000
    volumes:
      - ./config:/config
    command: ["-a", "radarr,sonarr"]
    restart: "no"
